export const MAX_WIDTH_ELEMENT = 70;
export const MAX_HEIGHT_ELEMENT = 70;
export const MIN_WIDTH_ELEMENT = 40;
export const PANEL_HEIGHT = 28;
export const MAIN_WRAPPER_WIDTH = window.innerWidth * 0.8;
export const MAIN_WRAPPER_HEIGHT = window.innerHeight * 0.8 - PANEL_HEIGHT * 2;

export const INIT_APP_OPTIONS = {
  width: MAIN_WRAPPER_WIDTH,
  height: MAIN_WRAPPER_HEIGHT,
  antialias: true,
  backgroundColor: 0xffffff
}

export const CANVAS_OPTIONS = {
  gravity: 3,
  shapesArea: 0,
  shapesPerSecond: 5,
  shapesQuantity: 0,
  timeDelay: 1000
}
